use std::collections::HashMap;

//mod scan;

struct ScanResults {
    results: HashMap<Str, ScanResult>
}

pub fn scan(scan_local: bool) {
//    let scan_paths = construct_search_paths(scan_local);

    
}
